# Инструкция по переходу на русские названия столбцов

Если у вас уже есть Google Sheets с английскими названиями столбцов, следуйте этой инструкции для перехода на русские названия.

## ⚠️ Важно!

Код поддерживает **обратную совместимость**. Вы можете:
1. Оставить английские названия - всё будет работать
2. Перейти на русские названия - для удобства заказчика
3. Использовать смешанный вариант (не рекомендуется)

---

## 🔄 Таблица соответствия

### Лист "Categories" → "Категории"

| Старое название (English) | Новое название (Русский) |
|---------------------------|--------------------------|
| `category_id` | `ID` |
| `category_name` | `Название` |
| `display_order` | `Порядок` |
| `emoji` | `Эмодзи` |

### Лист "Products" → "Товары"

| Старое название (English) | Новое название (Русский) |
|---------------------------|--------------------------|
| `product_id` | `ID товара` |
| `category` | `Категория` |
| `name` | `Название` |
| `description` | `Описание` |
| `wb_link` | `Ссылка WB` |
| `available_sizes` | `Размеры` |
| `photo_1_url` | `Фото 1` |
| `photo_2_url` | `Фото 2` |
| `photo_3_url` | `Фото 3` |
| `photo_4_url` | `Фото 4` |
| `collage_url` | `Коллаж` |
| `is_active` | `Активен` |
| `size_table_id` | `Таблица размеров` |

### Лист "Size_Tables" → "Размеры"

| Старое название (English) | Новое название (Русский) |
|---------------------------|--------------------------|
| `table_id` | `ID таблицы` |
| `size` | `Размер` |
| `height_min` | `Рост мин` |
| `height_max` | `Рост макс` |
| `chest_min` | `Грудь мин` |
| `chest_max` | `Грудь макс` |
| `waist_min` | `Талия мин` |
| `waist_max` | `Талия макс` |
| `hips_min` | `Бедра мин` |
| `hips_max` | `Бедра макс` |

---

## 📝 Пошаговая инструкция

### Шаг 1: Переименуйте листы

1. Откройте вашу Google Sheets таблицу
2. Переименуйте листы:
   - `Categories` → `Категории`
   - `Products` → `Товары`
   - `Size_Tables` → `Размеры`

**Как переименовать лист:**
- Кликните правой кнопкой на название листа внизу
- Выберите "Переименовать"
- Введите новое название

### Шаг 2: Переименуйте столбцы

Для каждого листа переименуйте заголовки столбцов (первая строка) согласно таблице соответствия выше.

**Пример для листа "Категории":**

Было:
```
category_id | category_name | display_order | emoji
```

Стало:
```
ID | Название | Порядок | Эмодзи
```

### Шаг 3: Обновите значения в столбце "Активен"

Если вы используете значения `TRUE`/`FALSE`, можете заменить их на `ДА`/`НЕТ`:

- `TRUE` → `ДА`
- `FALSE` → `НЕТ`

**Но это необязательно!** Код поддерживает оба варианта.

### Шаг 4: Проверьте работу

1. Перезапустите бота (или подождите обновления кеша)
2. Проверьте, что категории отображаются
3. Проверьте, что товары загружаются
4. Проверьте подбор размеров

---

## 🚀 Быстрый способ (для опытных пользователей)

Если вы хотите быстро переименовать все столбцы, используйте функцию "Найти и заменить" в Google Sheets:

1. Нажмите `Ctrl+H` (или `Cmd+H` на Mac)
2. Включите опцию "Искать в определенном диапазоне"
3. Выберите первую строку (строка с заголовками)
4. Замените каждое английское название на русское

**Внимание:** Будьте осторожны, чтобы не заменить данные в других строках!

---

## ✅ Проверочный список

После перехода на русские названия убедитесь:

- [ ] Все три листа переименованы
- [ ] Все заголовки столбцов переименованы
- [ ] Нет лишних пробелов в названиях столбцов
- [ ] Бот успешно загружает категории
- [ ] Бот успешно загружает товары
- [ ] Подбор размеров работает корректно

---

## ❓ Что делать, если что-то не работает?

### Проблема: Бот не видит категории

**Решение:**
1. Проверьте, что лист называется точно `Категории` (с заглавной буквы)
2. Проверьте, что столбцы называются точно как в таблице соответствия
3. Проверьте логи бота на наличие ошибок

### Проблема: Товары не отображаются

**Решение:**
1. Проверьте столбец `Активен` - должно быть `ДА`
2. Проверьте, что `Категория` соответствует `ID` из листа "Категории"
3. Очистите кеш командой `/admin_refresh_cache`

### Проблема: Не работает подбор размеров

**Решение:**
1. Проверьте, что столбец `Таблица размеров` в товаре соответствует `ID таблицы` в листе "Размеры"
2. Проверьте, что все числовые поля заполнены корректно
3. Проверьте логи на наличие ошибок парсинга

---

## 🔙 Откат изменений

Если что-то пошло не так, вы всегда можете вернуть английские названия обратно. Код поддерживает оба варианта!

---

## 📞 Поддержка

Если у вас возникли проблемы при переходе, обратитесь к разработчику.
