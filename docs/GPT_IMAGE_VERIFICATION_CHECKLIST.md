# Чеклист проверки GPT Image моделей в CometAPI

## Инструкция
Откройте документацию: **https://apidoc.cometapi.com/image-edits**

Заполните этот чеклист, сравнивая документацию с текущей реализацией.

---

## 1. ПРОВЕРКА НАЗВАНИЙ МОДЕЛЕЙ

### В документации найдите раздел с доступными моделями

- [ ] **Модель GPT Image -1:**
  - Название в документации: `_________________`
  - (убрано из проекта)

- [ ] **Модель GPT Image 1.5:**
  - Название в документации: `_________________`
  - Название в коде: `gpt-image-1.5` (строка 449 в `bot/handlers/tryon.py`)
  - ✅ Существует / ❌ Не существует
  - Если не существует, альтернатива: `_________________`

---

## 2. ПРОВЕРКА ENDPOINT

### В документации найдите endpoint для image-edits

- [ ] **Endpoint в документации:**
  - Формат: `_________________`
  - Пример: `_________________`

- [ ] **Endpoint в коде:**
  - Файл: `gpt_integration/photo_processing/image_client.py` (строка 207)
  - Текущий: `/v1beta/models/{model}:generateContent`
  - ✅ Совпадает / ❌ Не совпадает → **Исправить на:** `_________________`

---

## 3. ПРОВЕРКА ФОРМАТА ЗАПРОСА

### В документации найдите пример запроса

- [ ] **Структура запроса в документации:**
  ```json
  {
    "_________________": "_________________",
    "_________________": "_________________"
  }
  ```

- [ ] **Структура запроса в коде:**
  - Файл: `gpt_integration/photo_processing/image_client.py` (строки 194-204)
  - Текущий формат:
    ```python
    {
        "contents": [{"role": "user", "parts": [...]}],
        "generationConfig": {"responseModalities": ["TEXT", "IMAGE"]}
    }
    ```
  - ✅ Совпадает / ❌ Не совпадает

- [ ] **Если не совпадает, нужные изменения:**
  - `_________________`
  - `_________________`

---

## 4. ПРОВЕРКА ПАРАМЕТРОВ ЗАПРОСА

### В документации найдите все доступные параметры

- [ ] **Параметр `moderation`:**
  - В документации: ✅ Есть / ❌ Нет
  - В коде: ❌ Отсутствует (строка 201-203)
  - Значения: `_________________`
  - ✅ Добавить / ❌ Не требуется

- [ ] **Параметр разрешения изображения:**
  - В документации: ✅ Есть / ❌ Нет
  - Название параметра: `_________________`
  - Доступные значения: `_________________`
  - В коде: ❌ Отсутствует
  - ✅ Добавить / ❌ Не требуется

- [ ] **Другие параметры:**
  - `_________________`: `_________________`
  - `_________________`: `_________________`

---

## 5. ПРОВЕРКА ФОРМАТА ОТВЕТА

### В документации найдите пример ответа

- [ ] **Структура ответа в документации:**
  ```json
  {
    "_________________": "_________________",
    "_________________": "_________________"
  }
  ```

- [ ] **Структура ответа в коде:**
  - Файл: `gpt_integration/photo_processing/image_client.py` (строки 262-280)
  - Текущий парсинг ожидает:
    - `candidates[0].content.parts[].inlineData.data`
  - ✅ Совпадает / ❌ Не совпадает

- [ ] **Если не совпадает, путь к изображению в ответе:**
  - `_________________`

---

## 6. ПРОВЕРКА АВТОРИЗАЦИИ

### В документации найдите формат заголовка Authorization

- [ ] **Формат в документации:**
  - `Authorization: Bearer {api_key}` / `Authorization: {api_key}` / Другое: `_________________`

- [ ] **Формат в коде:**
  - Файл: `gpt_integration/photo_processing/image_client.py` (строка 58)
  - Текущий: `Authorization: {api_key}` (без "Bearer")
  - ✅ Совпадает / ❌ Не совпадает → **Исправить на:** `_________________`

---

## 7. ИТОГОВЫЙ СПИСОК ИЗМЕНЕНИЙ

### Запишите все необходимые изменения:

1. **Названия моделей:**
   - `_________________` → `_________________`
   - `_________________` → `_________________`

2. **Endpoint:**
   - `_________________` → `_________________`

3. **Формат запроса:**
   - Добавить параметры: `_________________`
   - Изменить структуру: `_________________`

4. **Формат ответа:**
   - Изменить парсинг: `_________________`

5. **Авторизация:**
   - Изменить формат: `_________________`

---

## 8. ПРОВЕРКА ТЕКУЩЕЙ РЕАЛИЗАЦИИ

### Файлы для проверки:

1. **`bot/handlers/tryon.py`**
   - Строки 444-451: Маппинг моделей
   - Проверить названия моделей

2. **`gpt_integration/photo_processing/image_client.py`**
   - Строка 58: Заголовок Authorization
   - Строка 207: Endpoint
   - Строки 194-204: Формат запроса
   - Строки 262-280: Парсинг ответа

3. **`gpt_integration/photo_processing/generator.py`**
   - Использование клиента
   - Обработка ошибок

---

## 9. РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

После заполнения чеклиста:

1. ✅ Если названия моделей неверны → Обновить в `bot/handlers/tryon.py`
2. ✅ Если endpoint другой → Обновить в `image_client.py`
3. ✅ Если формат запроса другой → Обновить структуру body в `image_client.py`
4. ✅ Если нужны дополнительные параметры → Добавить в `generationConfig`
5. ✅ Если формат ответа другой → Обновить парсинг в `image_client.py`
6. ✅ Если авторизация другая → Обновить заголовки в `image_client.py`

---

## 10. ТЕСТИРОВАНИЕ

После внесения изменений:

- [ ] Протестировать модель `gpt-image-1`
- [ ] Протестировать модель `gpt-image-1.5` (или альтернативу)
 - [ ] Протестировать модель `gpt-image-1.5`
- [ ] Проверить обработку ошибок
- [ ] Проверить качество генерации

---

## ПРИМЕЧАНИЯ

Запишите здесь любые дополнительные наблюдения из документации:

```
_________________
_________________
_________________
```
