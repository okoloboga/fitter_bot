## 1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

### 1.1 Назначение системы
Telegram-бот для женского бренда одежды, позволяющий пользователям просматривать каталог товаров, получать рекомендации по размеру на основе своих параметров и виртуально примерять одежду с помощью AI.

### 1.2 Этапность разработки
Проект разделен на два последовательных этапа:
- **Этап 1**: Каталог, навигация, подбор размеров, избранное
- **Этап 2**: AI-примерка одежды с использованием Gemini

### 1.3 Целевая аудитория
Женщины, заинтересованные в покупке одежды на Wildberries, желающие подобрать правильный размер и увидеть, как товар будет выглядеть на них.

---

## 2. ТЕХНОЛОГИЧЕСКИЙ СТЕК И ИНФРАСТРУКТУРА

### 2.1 Основные технологии
- **Telegram Bot**: Aiogram (асинхронная библиотека для работы с Telegram Bot API)
- **Backend API**: FastAPI (для обработки бизнес-логики, работы с данными)
- **База данных**: PostgreSQL (только для хранения пользовательских данных)
- **Хранилище каталога**: Google Sheets (источник данных о товарах)
- **AI-сервис**: Gemini API (для генерации примерок)

### 2.2 Инфраструктура
- Развертывание на существующем облачном сервере заказчика
- Изолированный проект, не требующий интеграции с другими системами
- Ожидаемая нагрузка: тысячи пользователей, до 100 товаров в каталоге

### 2.3 Архитектура системы

#### Компоненты системы:
1. **Telegram Bot (Aiogram)** - фронтенд-интерфейс, обработка команд и callback-запросов
2. **FastAPI Backend** - бизнес-логика, обработка данных, взаимодействие с внешними сервисами
3. **PostgreSQL** - хранение данных пользователей, избранного, параметров, истории примерок
4. **Google Sheets API** - чтение каталога товаров, таблиц размеров
5. **Gemini API** - генерация изображений с примеркой
6. **File Storage** - хранение фотографий пользователей и результатов примерок

#### Взаимодействие компонентов:
- Бот получает запросы от пользователя через Telegram API
- Бот обращается к FastAPI для получения данных и выполнения операций
- FastAPI читает данные о товарах из Google Sheets при необходимости
- FastAPI работает с PostgreSQL для пользовательских данных
- FastAPI взаимодействует с Gemini API для генерации примерок
- Результаты возвращаются пользователю через бота

---

## 3. СТРУКТУРА ДАННЫХ

### 3.1 База данных PostgreSQL

#### Таблица: users
Хранит основную информацию о пользователях.

**Поля:**
- id (первичный ключ, автоинкремент)
- tg_id (уникальный идентификатор Telegram, индексированный)
- username (имя пользователя в Telegram, может быть NULL)
- first_name (имя из профиля Telegram)
- created_at (дата и время первого взаимодействия с ботом)
- last_activity (дата и время последней активности)
- is_admin (булево значение, для доступа к статистике)

#### Таблица: user_measurements
Хранит параметры тела пользователя для подбора размеров.

**Поля:**
- `id` (первичный ключ)
- `user_id` (внешний ключ на users.id)
- `russian_size` (российский размер, например "42" или "42-44", строка)
- `shoulder_length` (длина плеч в см, целое число)
- `back_width` (ширина спины в см, целое число)
- `sleeve_length` (длина рукава в см, целое число)
- `back_length` (длина изделия по спинке в см, целое число)
- `chest` (обхват груди в см, целое число)
- `waist` (обхват талии в см, целое число)
- `hips` (обхват бедер в см, целое число)
- `pants_length` (длина брюк в см, целое число)
- `waist_girth` (обхват в поясе в см, целое число)
- `rise_height` (высота посадки в см, целое число)
- `back_rise_height` (высота посадки сзади в см, целое число)
- `updated_at` (дата и время последнего обновления параметров)

**Связь:** один к одному с users (один пользователь - один набор параметров)

#### Таблица: favorites
Хранит товары, добавленные пользователями в избранное.

**Поля:**
- id (первичный ключ)
- user_id (внешний ключ на users.id)
- product_id (идентификатор товара из Google Sheets, строка)
- added_at (дата и время добавления в избранное)

**Связь:** многие ко многим между users и товарами
**Индексы:** составной индекс на (user_id, product_id) для быстрого поиска

#### Таблица: user_photos
Хранит фотографии пользователей для примерки.

**Поля:**
- id (первичный ключ)
- user_id (внешний ключ на users.id)
- file_id (идентификатор файла в Telegram)
- file_path (путь к сохраненному файлу на сервере)
- uploaded_at (дата и время загрузки)
- is_active (булево значение, активно ли фото для использования)
- consent_given (булево значение, дано ли согласие на обработку)

**Связь:** один ко многим с users (пользователь может загрузить несколько фото)

#### Таблица: try_on_history
Хранит историю примерок для статистики.

**Поля:**
- id (первичный ключ)
- user_id (внешний ключ на users.id)
- product_id (идентификатор товара из Google Sheets)
- user_photo_id (внешний ключ на user_photos.id)
- result_file_path (путь к результату примерки)
- created_at (дата и время создания примерки)
- generation_time (время генерации в секундах)
- status (статус: success, failed, processing)

**Связь:** многие к одному с users и user_photos
**Индексы:** индекс на user_id, product_id, created_at для статистики

### 3.2 Google Sheets структура

#### Лист: Products (Товары)
Основная таблица с информацией о товарах.

**Столбцы:**
- product_id (уникальный идентификатор товара, строка, например: "jacket_001")
- category (категория товара, **используется для определения таблицы размеров**)
- name (название товара)
- description (описание товара, текст)
- wb_link (ссылка на товар на Wildberries)
- available_sizes (доступные размеры через запятую, например: "XS,S,M,L,XL")
- photo_1_url (ссылка на первое фото товара)
- photo_2_url (ссылка на второе фото товара)
- photo_3_url (ссылка на третье фото товара)
- photo_4_url (ссылка на четвертое фото товара)
- collage_url (ссылка на коллаж из 4 фотографий)
- is_active (булево значение: TRUE/FALSE, активен ли товар для показа)

**Примечания:**
- Все URL должны быть публично доступными (например, через Google Drive с общим доступом)
- Порядок строк определяет порядок отображения в категории

#### Лист: Categories (Категории)
Список категорий для навигации.

**Столбцы:**
- category_id (уникальный идентификатор категории)
- category_name (название категории для отображения пользователю)
- display_order (порядок отображения в меню, целое число)
- emoji (эмодзи для визуального оформления кнопки)

**Примечание:** около 8 категорий (различные виды верхней одежды + штаны)

#### Лист: Size_Tables (Таблицы размеров)
Таблицы соответствия параметров тела размерам одежды.

**Столбцы:**
- Категория (идентификатор таблицы, **должен соответствовать `category` товара**)
- size (размер: XS, S, M, L, XL, XXL и т.д.)
- russian_size (российский размер, например "42" или "42-44")
- chest_min / chest_max (обхват груди в см)
- waist_min / waist_max (обхват талии в см)
- hips_min / hips_max (обхват бедер в см)
- и другие параметры в формате `param_name_min` / `param_name_max`

**Примечания:**
- Для каждой категории товаров (`category_id`) должна быть соответствующая таблица размеров с таким же `table_id`.
- Диапазоны могут пересекаться для граничных случаев.
- Если параметр не важен для данного типа одежды, поля для него можно оставить пустыми.

---

## 4. ЭТАП 1: КАТАЛОГ И ПОДБОР РАЗМЕРОВ

### 4.1 Стартовое взаимодействие

#### Команда /start
**Поведение:**
- При первом запуске бота пользователь автоматически регистрируется в системе
- Система создает запись в таблице users с tg_id пользователя
- Бот отправляет приветственное сообщение с описанием возможностей
- Tone of voice: дружелюбный, молодежный
- Отображается главное меню с кнопками

**Главное меню содержит:**
- Кнопка "🛍 Каталог" - переход к выбору категории
- Кнопка "⭐️ Избранное" - просмотр избранных товаров
- Кнопка "📐 Мои параметры" - ввод/редактирование параметров тела
- Кнопка "ℹ️ О боте" - информация о возможностях бота

#### Повторный запуск /start
- Система проверяет наличие пользователя в базе
- Отображается главное меню
- Если пользователь уже вводил параметры, показывается краткая информация о них

### 4.2 Каталог товаров

#### Выбор категории
**Процесс:**
- Пользователь нажимает "🛍 Каталог"
- Бот запрашивает данные из Google Sheets (лист Categories)
- Отображается список категорий в виде inline-кнопок
- Каждая кнопка содержит эмодзи и название категории
- Порядок кнопок соответствует полю display_order
- Внизу кнопка "◀️ Назад" для возврата в главное меню

**Обработка данных:**
- FastAPI читает лист Categories из Google Sheets
- Данные кешируются на короткое время (например, 5 минут) для снижения нагрузки на API
- Фильтруются только активные категории, в которых есть активные товары

#### Просмотр товаров в категории
**Процесс:**
- Пользователь выбирает категорию
- Система загружает товары из Google Sheets (лист Products)
- Фильтрует товары по выбранной категории и is_active = TRUE
- Отображается первый товар с пагинацией

**Карточка товара содержит:**
- Коллаж из 4 фотографий (изображение из collage_url)
- Название товара
- Краткое описание (первые 200-300 символов, если описание длинное)
- Доступные размеры (отображаются как текст: "Размеры: XS, S, M, L, XL")
- Рекомендуемый размер (если пользователь ввел параметры)

**Кнопки под карточкой:**
- "⭐️ В избранное" / "❌ Убрать из избранного" (меняется в зависимости от статуса)
- "🔗 Открыть на WB" (ссылка на Wildberries)
- "📸 Посмотреть все фото" (открывает галерею из 4 фото)
- "👗 Примерить" (доступна на этапе 2)
- "◀️" и "▶️" (навигация между товарами)
- Счетчик: "Товар 3 из 12"
- "🔙 К категориям" (возврат к списку категорий)

#### Пагинация
**Логика:**
- Товары отображаются по одному
- Навигация через inline-кнопки "◀️" и "▶️"
- При достижении конца списка кнопка "▶️" становится неактивной или зацикливает на первый товар
- При нажатии на кнопки навигации сообщение редактируется (не создается новое)
- Состояние пагинации хранится в callback_data кнопок

**Технические детали:**
- Callback data содержит: тип действия, category_id, текущий индекс товара
- Формат: "product:category_id:index:action"
- Пример: "product:jackets:2:next"

#### Просмотр всех фото товара
**Процесс:**
- Пользователь нажимает "📸 Посмотреть все фото"
- Бот отправляет медиа-группу из 4 фотографий (photo_1_url, photo_2_url, photo_3_url, photo_4_url)
- После медиа-группы отправляется сообщение с кнопкой "◀️ Вернуться к товару"
- При возврате снова показывается карточка товара с коллажем

### 4.3 Система подбора размеров

#### Ввод параметров пользователя

**Процесс первого ввода:**
- Пользователь нажимает "📐 Мои параметры" в главном меню
- Если параметры еще не введены, бот запускает пошаговый опрос
- Если параметры уже есть, показывается текущая информация с кнопками "✏️ Изменить" и "◀️ Назад"

**Пошаговый опрос:**
Процесс ввода всех параметров (российский размер, обхваты, длины и т.д.) описан в детальной документации.

**После завершения опроса:**
- Система сохраняет данные в таблицу user_measurements
- Отображается сводка с сохраненными параметрами
- Кнопки: "✏️ Изменить", "◀️ В главное меню"

**Технические детали:**
- Используется FSM (Finite State Machine) в Aiogram для управления состояниями диалога
- Данные временно хранятся в состоянии, затем сохраняются в БД

#### Изменение параметров
**Процесс:**
- Пользователь нажимает "✏️ Изменить"
- Отображаются кнопки для выбора параметра для изменения
- После выбора запускается ввод только этого параметра
- После ввода показывается обновленная сводка

#### Логика рекомендации размера

**Алгоритм подбора:**
1. Система получает параметры пользователя из таблицы user_measurements.
2. Определяет `category` для текущего товара из Google Sheets (Products).
3. Загружает соответствующую таблицу размеров из листа Size_Tables, используя `category` как идентификатор таблицы.
4. Для каждого размера проверяет, попадают ли параметры пользователя в диапазоны.
5. Считает количество совпадений по всем релевантным параметрам (российский размер, обхваты, длины и т.д.).

**Правила определения рекомендуемого размера:**
- Идеальное совпадение: большинство параметров попадают в диапазон одного размера.
- Частичное совпадение: рекомендуется размер с наибольшим количеством совпадений.
- Граничный случай: параметры находятся между двумя размерами.

**Отображение рекомендации:**
- Если есть идеальное совпадение: "✅ Рекомендуемый размер: M"
- Если граничный случай: "✅ Рекомендуемый размер: M (также может подойти L)"
- Если параметры не введены: "📐 Укажи свои параметры, чтобы получить рекомендацию по размеру"
- Если не удалось подобрать размер: "⚠️ Не удалось подобрать размер. Рекомендуем уточнить у продавца"

**Технические детали:**
- Логика подбора реализуется в FastAPI
- Endpoint: POST /api/recommend-size
- Параметры запроса: user_id, product_id
- Ответ: recommended_size, alternative_size (опционально), confidence_level

### 4.4 Избранное

#### Добавление в избранное
**Процесс:**
- Пользователь просматривает карточку товара
- Нажимает кнопку "⭐️ В избранное"
- Система создает запись в таблице favorites
- Кнопка меняется на "❌ Убрать из избранного"
- Показывается краткое уведомление: "Товар добавлен в избранное ⭐️"

**Технические детали:**
- Проверка на дубликаты: если товар уже в избранном, не создается новая запись
- Callback data: "fav:add:product_id"

#### Удаление из избранного
**Процесс:**
- Пользователь нажимает "❌ Убрать из избранного"
- Система удаляет запись из таблицы favorites
- Кнопка меняется на "⭐️ В избранное"
- Уведомление: "Товар удален из избранного"

#### Просмотр избранного
**Процесс:**
- Пользователь нажимает "⭐️ Избранное" в главном меню
- Система загружает список product_id из таблицы favorites для данного пользователя
- Загружает информацию о товарах из Google Sheets
- Отображает товары в том же формате, что и в каталоге (карточки с пагинацией)
- Если избранное пустое: "Твое избранное пока пусто. Добавь понравившиеся товары из каталога! 💫"

**Особенности:**
- Порядок отображения: от последнего добавленного к первому (по полю added_at)
- Все функции карточки товара доступны (примерка, переход на WB, удаление из избранного)
- Кнопка "🔙 В главное меню" для возврата

---

## 5. ЭТАП 2: AI-ПРИМЕРКА ОДЕЖДЫ

### 5.1 Загрузка фото пользователя

#### Первая примерка
**Процесс:**
- Пользователь просматривает карточку товара
- Нажимает кнопку "👗 Примерить"
- Если у пользователя нет сохраненного фото, запускается процесс загрузки
- Если фото уже есть, предлагается выбор: использовать существующее или загрузить новое

**Запрос на загрузку фото:**
- Сообщение: "Чтобы примерить одежду, загрузи свое фото в полный рост на однотонном фоне. Убедись, что ты стоишь прямо и тебя хорошо видно 📸"
- Кнопка "ℹ️ Требования к фото" - показывает подробные рекомендации
- Кнопка "◀️ Отмена" - возврат к карточке товара

**Требования к фото (детальное описание):**
- Фото в полный рост
- Однотонный фон (желательно светлый)
- Хорошее освещение
- Фронтальный ракурс (лицом к камере)
- Руки вдоль тела или слегка в стороны
- Формат: JPG, PNG
- Размер файла: не более 10 МБ

#### Согласие на обработку фото
**Процесс:**
- Перед первой загрузкой фото пользователь должен дать согласие
- Отображается сообщение с информацией об обработке данных
- Текст согласия: "Для работы примерки нам нужно обработать твое фото с помощью AI. Мы сохраним фото для повторного использования, чтобы тебе не пришлось загружать его каждый раз. Фото используется только для примерки и не передается третьим лицам. Согласен(на)?"
- Кнопки: "✅ Согласен", "❌ Отказаться"

**При согласии:**
- Устанавливается флаг consent_given = TRUE
- Запускается процесс загрузки фото

**При отказе:**
- Возврат к карточке товара
- Функция примерки остается недоступной до получения согласия

#### Валидация загруженного фото

**Автоматическая проверка:**
После загрузки фото система выполняет проверки:

1. **Технические проверки:**
   - Формат файла (JPG, PNG)
   - Размер файла (не более 10 МБ)
   - Разрешение изображения (минимум 512x512 пикселей)

2. **AI-проверка содержимого:**
   - Используется Gemini API для анализа изображения
   - Проверяется наличие человека в кадре
   - Определяется, виден ли человек в полный рост
   - Оценивается качество фото (освещение, четкость)
   - Определяется ракурс (фронтальный, боковой, другой)

**Формат запроса к Gemini для валидации:**
- Промпт: "Проанализируй это изображение. Определи: 1) Есть ли на фото человек? 2) Виден ли человек в полный рост? 3) Какой ракурс (фронтальный/боковой/другой)? 4) Подходит ли фото для виртуальной примерки одежды? Ответь в формате JSON."
- Ожидаемый ответ: структурированные данные с булевыми значениями и описанием

**Обработка результатов валидации:**

- **Если фото подходит:**
  - Сохранение в таблицу user_photos
  - Сохранение файла на сервере
  - Сообщение: "Отлично! Фото принято ✅ Теперь запускаем примерку..."
  - Автоматический запуск процесса примерки

- **Если фото не подходит:**
  - Сообщение с объяснением проблемы:
    - "На фото не обнаружен человек. Пожалуйста, загрузи фото, где ты видна в полный рост"
    - "Фото слишком темное или размытое. Попробуй сделать фото при хорошем освещении"
    - "На фото видна только часть тела. Нужно фото в полный рост"
  - Кнопки: "📸 Загрузить другое фото", "◀️ Отмена"

### 5.2 Анализ позы и выбор фото товара

#### Определение ракурса пользователя
**Процесс:**
- После успешной валидации фото анализируется поза пользователя
- Gemini API определяет ракурс: фронтальный (лицом к камере) или боковой
- Результат сохраняется в метаданных фото

**Использование информации о ракурсе:**
- Система выбирает наиболее подходящее фото товара для примерки
- Если пользователь на фото в фронтальном ракурсе, используется фронтальное фото товара (обычно photo_1 или photo_2)
- Если в боковом - боковое фото товара (если доступно)

**Технические детали:**
- Информация о ракурсе каждого фото товара может быть указана в Google Sheets (дополнительные столбцы: photo_1_angle, photo_2_angle и т.д.)
- Значения: "front", "side", "back", "other"
- Если информация о ракурсе не указана, используется photo_1 по умолчанию

### 5.3 Генерация примерки

#### Подготовка запроса к Gemini
**Процесс:**
1. Система получает:
   - Фото пользователя из user_photos
   - Выбранное фото товара на основе ракурса
   - Информацию о товаре (название, описание)

2. Формируется запрос к Gemini API:
   - Входные данные: фото пользователя, фото товара
   - Промпт для генерации: детальное описание задачи виртуальной примерки
   - Параметры генерации: качество, стиль, сохранение черт лица

**Структура промпта:**
"Создай реалистичное изображение, где человек с первого фото одет в одежду со второго фото. Сохрани:
- Черты лица и прическу человека
- Позу и положение тела
- Фон из оригинального фото
- Реалистичную посадку одежды на фигуре
- Освещение и тени, соответствующие оригинальному фото
Одежда должна выглядеть естественно, с учетом складок и драпировки ткани."

#### Процесс генерации
**Технические детали:**
- Создается запись в try_on_history со статусом "processing"
- Отправляется запрос к Gemini API
- Время генерации: около 3 минут
- Во время ожидания пользователь видит сообщение с индикацией процесса

**Сообщение во время генерации:**
- Текст: "Создаем твою примерку... Это займет около 3 минут ⏳"
- Можно добавить анимированный эмодзи или прогресс-бар (через редактирование сообщения каждые 30 секунд)
- Примеры промежуточных сообщений:
  - "Анализируем фото... 🔍"
  - "Подбираем одежду... 👗"
  - "Создаем финальное изображение... ✨"

#### Обработка результата

**При успешной генерации:**
- Gemini возвращает изображение
- Изображение сохраняется на сервере
- Обновляется запись в try_on_history: status = "success", result_file_path, generation_time
- Пользователю отправляется результат

**Формат отправки результата:**
- Изображение с примеркой
- Подпись: "Вот как на тебе будет смотреться [название товара]! 💫"
- Кнопки:
  - "🔗 Купить на WB" (ссылка на товар)
  - "💾 Сохранить результат" (отправка файла для скачивания)
  - "🔄 Попробовать другое фото"
  - "👗 Примерить другой товар"
  - "◀️ Вернуться к товару"

**При ошибке генерации:**
- Обновляется запись в try_on_history: status = "failed"
- Сообщение пользователю: "Не удалось создать примерку 😔 Попробуй загрузить другое фото или выбрать другой товар"
- Кнопки: "📸 Загрузить другое фото", "◀️ Вернуться к товару"

### 5.4 Сохранение и повторное использование фото

#### Управление сохраненными фото
**Процесс:**
- После первой успешной загрузки фото сохраняется с флагом is_active = TRUE
- При следующей примерке пользователю предлагается выбор:
  - "Использовать сохраненное фото"
  - "Загрузить новое фото"

**Если пользователь выбирает сохраненное фото:**
- Показывается превью сохраненного фото
- Кнопки: "✅ Использовать это фото", "📸 Загрузить новое"
- При подтверждении сразу запускается генерация примерки

**Если загружается новое фото:**
- Предыдущее фото остается в базе, но is_active меняется на FALSE
- Новое фото становится активным
- Запускается процесс валидации и генерации

**Доступ к истории фото:**
- В разделе "📐 Мои параметры" добавляется кнопка "📸 Мои фото"
- Отображается список сохраненных фото с датами загрузки
- Возможность выбрать активное фото или удалить старые

### 5.5 Сохранение результатов примерки

#### Функция "Сохранить результат"
**Процесс:**
- Пользователь нажимает "💾 Сохранить результат"
- Бот отправляет изображение как документ (не сжатое)
- Сообщение: "Результат примерки сохранен! Теперь ты можешь скачать его в галерею 📥"

**Доступ к истории примерок:**
- В главном меню добавляется кнопка "📜 История примерок" (появляется после первой примерки)
- Отображается список товаров, которые пользователь примерял
- Для каждого товара: миниатюра результата, название товара, дата примерки
- При выборе открывается полноразмерное изображение с кнопками:
  - "🔗 Купить на WB"
  - "💾 Скачать"
  - "🗑 Удалить из истории"

---

## 6. АДМИНИСТРАТИВНАЯ ПАНЕЛЬ

### 6.1 Доступ к админ-функциям

#### Команда /admin_stats
**Процесс:**
- Пользователь отправляет команду /admin_stats
- Система проверяет поле is_admin в таблице users для данного tg_id
- Если is_admin = TRUE, отображается статистика
- Если is_admin = FALSE, отправляется сообщение: "У тебя нет доступа к этой команде"

**Назначение администраторов:**
- Администраторы назначаются вручную через прямое изменение поля is_admin в базе данных
- Или через специальную команду /make_admin [tg_id], доступную только владельцу бота (tg_id владельца жестко закодирован)

### 6.2 Статистика

#### Общая статистика пользователей
**Метрики:**
- Общее количество пользователей (COUNT из таблицы users)
- Количество новых пользователей за сегодня
- Количество новых пользователей за последние 7 дней
- Количество новых пользователей за последние 30 дней
- Количество активных пользователей (с активностью за последние 7 дней)

#### Статистика примерок
**Метрики:**
- Общее количество примерок (COUNT из try_on_history со статусом success)
- Количество примерок за сегодня
- Количество примерок за последние 7 дней
- Количество примерок за последние 30 дней
- Среднее время генерации примерки
- Процент успешных примерок (success / (success + failed) * 100)

#### Статистика по товарам

**Самые примеряемые товары:**
- ТОП-10 товаров по количеству примерок
- Формат: название товара, количество примерок
- Запрос: GROUP BY product_id, COUNT(*), JOIN с данными из Google Sheets для получения названий

**Самые избранные товары:**
- ТОП-10 товаров по количеству добавлений в избранное
- Формат: название товара, количество добавлений
- Запрос: GROUP BY product_id, COUNT(*) из таблицы favorites

#### Формат отображения статистики
**Структура сообщения:**
```
📊 Статистика бота

👥 Пользователи:
Всего: 1234
Новых за сегодня: 15
Новых за 7 дней: 89
Новых за 30 дней: 345
Активных за 7 дней: 567

👗 Примерки:
Всего: 3456
За сегодня: 42
За 7 дней: 298
За 30 дней: 1123
Среднее время генерации: 2.8 мин
Успешных: 95.2%

🔥 ТОП-5 примеряемых товаров:
1. Куртка оверсайз черная - 234 примерки
2. Джинсы mom fit синие - 198 примерок
3. Пальто шерстяное бежевое - 176 примерок
4. Худи с принтом - 154 примерки
5. Брюки карго хаки - 142 примерки

⭐️ ТОП-5 избранных товаров:
1. Куртка оверсайз черная - 456 добавлений
2. Пальто шерстяное бежевое - 389 добавлений
3. Джинсы mom fit синие - 312 добавлений
4. Платье миди черное - 287 добавлений
5. Худи с принтом - 245 добавлений
```

**Кнопки:**
- "🔄 Обновить" (перезагружает статистику)
- "📅 Детальная статистика" (опционально, для будущего расширения)

### 6.3 Дополнительные админ-команды (опционально)

#### /admin_users
Список последних зарегистрированных пользователей (последние 20)

#### /admin_errors
Лог последних ошибок (failed примерки, ошибки валидации)

---

## 7. ИНТЕГРАЦИЯ С GOOGLE SHEETS

### 7.1 Подключение к Google Sheets API

**Требования:**
- Использование Google Sheets API v4
- Аутентификация через Service Account
- JSON-ключ сервисного аккаунта хранится в защищенном месте на сервере
- Таблица Google Sheets должна иметь права доступа для сервисного аккаунта

**Технические детали:**
- Библиотека для работы с API (например, gspread для Python)
- Настройка OAuth 2.0 credentials
- Обработка ошибок доступа и лимитов API

### 7.2 Стратегия чтения данных

#### Кеширование
**Цель:** Минимизировать количество запросов к Google Sheets API и ускорить работу бота.

**Стратегия:**
- Данные о категориях кешируются на 10 минут
- Данные о товарах кешируются на 5 минут
- Таблицы размеров кешируются на 30 минут
- Кеш хранится в памяти приложения (например, с использованием библиотеки cachetools)
- При изменении данных в Google Sheets они обновятся в боте после истечения времени кеша

**Альтернативный подход:**
- Использование Redis для кеширования (если требуется масштабирование)
- Возможность принудительного сброса кеша через админ-команду /admin_refresh_cache

#### Обработка ошибок
**Сценарии:**
- Google Sheets API недоступен
- Превышен лимит запросов
- Таблица удалена или изменены права доступа
- Некорректная структура данных в таблице

**Действия при ошибках:**
- Логирование ошибки
- Использование закешированных данных (если доступны)
- Отправка уведомления администратору (опционально)
- Показ пользователю сообщения: "Каталог временно недоступен. Попробуй позже"

### 7.3 Валидация данных из Google Sheets

**Проверки при загрузке:**
- Наличие обязательных полей (product_id, name, category)
- Корректность URL (фото, ссылка на WB)
- Корректность формата размеров
- Уникальность product_id

**Обработка некорректных данных:**
- Товары с некорректными данными пропускаются
- Логируется предупреждение с указанием строки и проблемы
- Администратор может получить отчет через /admin_errors

---

## 8. ИНТЕГРАЦИЯ С GEMINI API

### 8.1 Подключение к Gemini

**Требования:**
- API ключ для доступа к Gemini
- Endpoint для запросов (уточняется в документации Gemini)
- Обработка асинхронных запросов (генерация занимает ~3 минуты)

**Технические детали:**
- Использование асинхронных HTTP-запросов (aiohttp)
- Таймаут запроса: 5 минут (с учетом времени генерации)
- Retry-логика при временных ошибках (максимум 2 повторные попытки)

### 8.2 Форматы запросов

#### Валидация фото
**Запрос:**
- Тип: анализ изображения
- Входные данные: изображение пользователя
- Промпт: запрос на определение наличия человека, ракурса, качества фото
- Ожидаемый формат ответа: JSON с булевыми значениями и описаниями

**Пример структуры ответа:**
```
{
  "has_person": true,
  "full_body_visible": true,
  "angle": "front",
  "suitable_for_tryon": true,
  "quality_issues": []
}
```

#### Генерация примерки
**Запрос:**
- Тип: генерация изображения
- Входные данные: фото пользователя, фото товара
- Промпт: детальное описание задачи виртуальной примерки
- Параметры: качество изображения, стиль (реалистичный)

**Обработка ответа:**
- Получение URL сгенерированного изображения или бинарных данных
- Скачивание и сохранение изображения на сервере
- Проверка качества результата (размер файла, формат)

### 8.3 Обработка ошибок Gemini API

**Типы ошибок:**
- Таймаут (генерация превысила 5 минут)
- Ошибка API (недоступность сервиса)
- Некорректный результат (пустое изображение, ошибка обработки)
- Превышение лимитов (если есть ограничения по количеству запросов)

**Действия при ошибках:**
- Логирование ошибки с деталями запроса
- Обновление статуса в try_on_history на "failed"
- Уведомление пользователя с предложением повторить попытку
- Уведомление администратора при повторяющихся ошибках

---

## 9. ПОЛЬЗОВАТЕЛЬСКИЙ ИНТЕРФЕЙС И UX

### 9.1 Tone of Voice

**Характеристики:**
- Дружелюбный и молодежный стиль общения
- Использование эмодзи для визуального оформления
- Обращение на "ты"
- Позитивный и поддерживающий тон
- Короткие и понятные сообщения

**Примеры формулировок:**
- Вместо "Произошла ошибка" → "Упс, что-то пошло не так 😅"
- Вместо "Загрузите фотографию" → "Загрузи свое фото 📸"
- Вместо "Параметры сохранены" → "Отлично! Твои параметры сохранены ✨"

### 9.2 Обработка ошибок и edge cases

#### Сетевые ошибки
- Сообщение: "Кажется, у нас проблемы с соединением. Попробуй еще раз через минутку 🔄"
- Кнопка "Повторить"

#### Некорректный ввод
- Четкое объяснение, что именно не так
- Пример правильного ввода
- Возможность вернуться назад

#### Долгое ожидание
- Информирование пользователя о процессе
- Промежуточные сообщения во время генерации
- Возможность отменить операцию

### 9.3 Навигация

**Принципы:**
- Всегда есть кнопка "Назад" или "В главное меню"
- Контекстные кнопки (релевантные текущему экрану)
- Минимум текстового ввода, максимум кнопок
- Логичная структура переходов

**Breadcrumbs (хлебные крошки):**
- В сложных разделах показывать текущее местоположение
- Пример: "Каталог → Куртки → Куртка оверсайз черная"

### 9.4 Адаптивность интерфейса

**Учет состояния пользователя:**
- Если параметры не введены, показывать напоминание при просмотре товаров
- Если фото не загружено, кнопка "Примерить" ведет на загрузку фото
- Если избранное пустое, показывать мотивирующее сообщение

**Персонализация:**
- Использование имени пользователя (если доступно в Telegram)
- Запоминание последней просмотренной категории
- Быстрый доступ к часто используемым функциям

---

## 10. БЕЗОПАСНОСТЬ И ПРИВАТНОСТЬ

### 10.1 Хранение данных

**Персональные данные:**
- Хранятся только необходимые данные: tg_id, username, параметры тела
- Фотографии хранятся в зашифрованном виде на сервере
- Доступ к базе данных защищен паролем
- Регулярные бэкапы базы данных

**Согласие на обработку:**
- Явное согласие пользователя на обработку фото
- Возможность отозвать согласие и удалить данные

### 10.2 Удаление данных

**Команда /delete_my_data:**
- Пользователь может запросить удаление всех своих данных
- Подтверждение действия (защита от случайного удаления)
- Удаляются: параметры, фото, история примерок, избранное
- Запись в users остается для статистики, но обезличивается

**Процесс удаления:**
- Сообщение: "Ты уверен(а), что хочешь удалить все свои данные? Это действие нельзя отменить."
- Кнопки: "✅ Да, удалить", "❌ Отмена"
- После удаления: "Все твои данные удалены. Ты можешь начать заново в любой момент!"

### 10.3 Защита от злоупотреблений

**Rate limiting:**
- Ограничение на количество примерок в день (например, 10 примерок на пользователя)
- Ограничение на частоту запросов (защита от спама)
- При превышении лимита: "Ты достиг(ла) лимита примерок на сегодня. Попробуй завтра! 😊"

**Модерация контента:**
- Проверка загружаемых фото на неприемлемый контент (опционально, через Gemini)
- Блокировка пользователей при нарушениях (вручную администратором)

---

## 11. ПРОИЗВОДИТЕЛЬНОСТЬ И МАСШТАБИРОВАНИЕ

### 11.1 Оптимизация запросов

**База данных:**
- Индексы на часто запрашиваемые поля (tg_id, product_id, created_at)
- Использование connection pooling для PostgreSQL
- Оптимизация сложных запросов (например, для статистики)

**Google Sheets:**
- Кеширование данных
- Батчинг запросов (загрузка нескольких товаров за один запрос)
- Использование фильтров на стороне API

**Gemini API:**
- Очередь запросов для управления нагрузкой
- Асинхронная обработка (пользователь не блокируется во время генерации)

### 11.2 Мониторинг

**Метрики для отслеживания:**
- Время ответа бота на команды
- Количество ошибок API (Google Sheets, Gemini)
- Использование памяти и CPU
- Размер базы данных
- Количество активных пользователей

**Логирование:**
- Все ошибки логируются с контекстом
- Критические ошибки отправляют уведомления администратору
- Логи хранятся с ротацией (например, последние 30 дней)

### 11.3 Масштабирование

**Горизонтальное масштабирование:**
- Возможность запуска нескольких экземпляров бота
- Использование Redis для shared state (если потребуется)
- Load balancing для FastAPI

**Вертикальное масштабирование:**
- Увеличение ресурсов сервера при росте нагрузки
- Оптимизация кода и запросов

---

## 12. РАЗВЕРТЫВАНИЕ И ПОДДЕРЖКА

### 12.1 Окружения

**Development (разработка):**
- Локальная разработка
- Тестовый бот в Telegram
- Тестовая база данных
- Моки для внешних API (опционально)

**Production (продакшн):**
- Облачный сервер заказчика
- Продакшн база данных с бэкапами
- Реальные API ключи
- Мониторинг и логирование

### 12.2 Конфигурация

**Переменные окружения:**
- TELEGRAM_BOT_TOKEN
- DATABASE_URL (PostgreSQL connection string)
- GOOGLE_SHEETS_CREDENTIALS (путь к JSON-ключу)
- GOOGLE_SHEETS_ID (ID таблицы)
- GEMINI_API_KEY
- GEMINI_API_ENDPOINT
- ADMIN_TG_IDS (список tg_id администраторов)
- FILE_STORAGE_PATH (путь для хранения фото)

**Конфигурационный файл:**
- Настройки кеширования
- Лимиты и таймауты
- Тексты сообщений (для легкой локализации в будущем)

### 12.3 Процесс развертывания

**Шаги:**
1. Настройка сервера и установка зависимостей
2. Создание базы данных PostgreSQL и применение миграций
3. Настройка Google Sheets API и получение credentials
4. Настройка Gemini API
5. Конфигурация переменных окружения
6. Запуск FastAPI сервера
7. Запуск Telegram бота
8. Настройка автозапуска (systemd, supervisor и т.д.)
9. Настройка мониторинга и логирования
10. Тестирование основных сценариев

### 12.4 Обновления

**Процесс обновления:**
- Создание бэкапа базы данных
- Остановка бота
- Применение миграций БД (если есть)
- Обновление кода
- Запуск бота
- Проверка работоспособности

**Миграции базы данных:**
- Использование инструмента для миграций (например, Alembic для Python)
- Версионирование схемы БД
- Rollback plan на случай проблем

---

## 13. ТЕСТИРОВАНИЕ

### 13.1 Типы тестирования

**Unit-тесты:**
- Тестирование логики подбора размеров
- Валидация данных из Google Sheets
- Обработка ошибок

**Интеграционные тесты:**
- Взаимодействие с Google Sheets API
- Взаимодействие с Gemini API
- Работа с базой данных

**E2E тесты (end-to-end):**
- Полные пользовательские сценарии
- Тестирование через Telegram Bot API

### 13.2 Тестовые сценарии

**Основные сценарии:**
1. Регистрация нового пользователя
2. Ввод параметров тела
3. Просмотр каталога и навигация
4. Добавление товара в избранное
5. Получение рекомендации по размеру
6. Загрузка фото и валидация
7. Генерация примерки
8. Просмотр истории примерок
9. Удаление данных

**Граничные случаи:**
- Некорректный ввод параметров
- Загрузка неподходящего фото
- Ошибки внешних API
- Превышение лимитов
- Пустое избранное, пустая история

### 13.3 Бета-тестирование

**Процесс:**
- Закрытое бета-тестирование с ограниченным числом пользователей
- Сбор обратной связи
- Мониторинг ошибок и производительности
- Итерации и улучшения перед публичным запуском

---

## 14. ДОКУМЕНТАЦИЯ

### 14.1 Техническая документация

**Содержание:**
- Архитектура системы (диаграммы)
- Описание API endpoints (FastAPI)
- Схема базы данных
- Структура Google Sheets
- Инструкции по развертыванию
- Инструкции по обновлению

### 14.2 Пользовательская документация

**Содержание:**
- Руководство по использованию бота
- FAQ (часто задаваемые вопросы)
- Требования к фото для примерки
- Как правильно измерить параметры тела
- Политика конфиденциальности

### 14.3 Документация для администраторов

**Содержание:**
- Как добавлять/редактировать товары в Google Sheets
- Интерпретация статистики
- Решение типичных проблем
- Контакты для технической поддержки

---

## 15. БУДУЩИЕ РАСШИРЕНИЯ (POST-MVP)

### 15.1 Возможные улучшения

**Функциональные:**
- Уведомления о новых товарах
- Уведомления о снижении цен на избранные товары
- Возможность поделиться результатом примерки
- Интеграция с другими маркетплейсами (Ozon, Lamoda)
- Рекомендации товаров на основе истории просмотров
- Виртуальный стилист (подбор образов)

**Технические:**
- Многоязычность
- Веб-версия каталога
- Мобильное приложение
- Интеграция с CRM
- Расширенная аналитика (когортный анализ, воронки)

### 15.2 Приоритизация

**Критерии:**
- Ценность для пользователей
- Сложность реализации
- Влияние на бизнес-метрики
- Обратная связь от пользователей

---

## 16. КРИТЕРИИ ПРИЕМКИ

### 16.1 Этап 1

**Функциональность:**
- ✅ Пользователь может просмотреть все категории товаров
- ✅ Пользователь может просмотреть товары внутри категории с пагинацией
- ✅ Карточка товара содержит все необходимые элементы (фото, описание, размеры, ссылку на WB)
- ✅ Пользователь может ввести свои параметры
- ✅ Система корректно рекомендует размер на основе параметров
- ✅ Пользователь может добавить/удалить товары из избранного
- ✅ Раздел "Избранное" отображает сохраненные товары
- ✅ Администратор может просмотреть статистику через /admin_stats

**Качество:**
- ✅ Бот отвечает на команды без задержек (< 2 секунд)
- ✅ Данные из Google Sheets загружаются корректно
- ✅ Обработка ошибок работает корректно
- ✅ Интерфейс интуитивно понятен

### 16.2 Этап 2

**Функциональность:**
- ✅ Пользователь может загрузить фото для примерки
- ✅ Система валидирует фото (наличие человека, качество)
- ✅ Генерация примерки работает через Gemini API
- ✅ Результат примерки отображается пользователю
- ✅ Пользователь может сохранить результат примерки
- ✅ Фото пользователя сохраняется для повторного использования
- ✅ История примерок доступна для просмотра
- ✅ Статистика по примеркам отображается в админ-панели

**Качество:**
- ✅ Генерация примерки завершается за приемлемое время (~3 минуты)
- ✅ Качество сгенерированного изображения удовлетворительное
- ✅ Обработка ошибок Gemini API работает корректно
- ✅ Пользовательский опыт плавный и понятный

---

## 17. ГЛОССАРИЙ

**Термины:**
- **tg_id** - уникальный идентификатор пользователя в Telegram
- **product_id** - уникальный идентификатор товара в Google Sheets
- **Примерка (try-on)** - процесс генерации изображения пользователя в выбранной одежде
- **Коллаж** - изображение, состоящее из 4 фотографий товара
- **Ракурс** - угол съемки (фронтальный, боковой)
- **FSM (Finite State Machine)** - конечный автомат для управления состояниями диалога
- **Callback data** - данные, передаваемые при нажатии inline-кнопки
- **Rate limiting** - ограничение частоты запросов

---

## 18. КОНТАКТЫ И ОТВЕТСТВЕННОСТЬ

**Роли в проекте:**
- **Заказчик** - владелец бренда, предоставляет данные о товарах, утверждает функциональность
- **Разработчик** - реализует бота согласно ТЗ
- **Администратор** - управляет каталогом через Google Sheets, мониторит статистику

**Коммуникация:**
- Регулярные созвоны для обсуждения прогресса
- Демонстрация промежуточных результатов
- Сбор обратной связи на каждом этапе

